[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "oncoforge"
version = "0.7.0"
description = "Evidence-governed cancer target discovery and conceptual systems research engine."
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "R&D BioTech Alaska" }
]
license = { text = "MIT" }
keywords = ["simulation", "cancer-systems", "conceptual-modeling", "quantum-strategy"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Topic :: Scientific/Engineering",
]
dependencies = []

[project.optional-dependencies]
qsa = ["qubit-state-algebra>=0.1.7,<0.3"]

[project.scripts]
oncoforge = "oncoforge.cli:main"

[tool.setuptools.packages.find]
include = ["oncoforge*"]

[tool.setuptools.package-data]
oncoforge = ["data/*.json"]
